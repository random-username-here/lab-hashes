Пишем хэш-таблицы с открытыми цепочками
будут храниться слова
скажем из толкиена, или войны и мира
load factor 1.5/2 по хорошему, но мы будем брать 15/20

берём файл, подготавливаем его (чтобы грузить было легко),
кладём в таблицу. 
Затем делаем много (например 10 000) поисков.
Не указываем оптимизаций.

Это первый шаг, его замеряем.

Затем добавляем -O2 или -O3 и снова меряем.

Ищем узкое место в реализцаии таблиц, переписываем её на асме
и так до тех пор, пока выигрыш станет незначительным

Обычно останавливаются на 0.5%. Мы останавливаемся после 4-5 стадий оптимизации.
Последнюю отбрасываем.

Варианты, которые должны быть:

	- интринсики
	- полное переписывание
	- встроенный ассемблер

на хуавей для олимпиадников отдельные квоты
есть ещё байкал, про него в четверг
	но потом будет сложно получать диплом

---

можно пользоваться perf	

valgrind -- виртуальная машина
к нему плагин callgrind
вызывается `valgrind --to callgrind`
профилирует вызовы
`ccachegrind` для кэшей

---

формальнее readme
O3 сразу
ещё один шаг возможно
табличка вместе с графиком
вывод

факториал

